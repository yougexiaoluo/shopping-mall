.search_input_wrapper {
  background: var(--themeColor);
  padding: 5px;
}

.navigator {
  display: flex;
  align-items: center;
  justify-content: center;
  letter-spacing: 5px;
  padding: 5px 0;
  color: var(--color999);
  border-radius: 5px;
  background: #fff;
  font-size: 14px;
}